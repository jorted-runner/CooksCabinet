//
//  Constants.swift
//  CooksCabinet
//
//  Created by Danny Ellis on 2/13/25.
//
//  This file defines global constants used throughout the app.
//  Constants are stored in an enum to prevent accidental instantiation.
//

import UIKit

/// A collection of global constants used across the app.
/// - Stored as an `enum` to prevent instantiation.
enum Constants {
    /// Default placeholder image used when a recipe has no image.
    static let placeholder = UIImage(systemName: "photo.fill")!
}
